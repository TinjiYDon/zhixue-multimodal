# M01 · Vue 3 前端（队友 A）

## 目标

课堂时间轴播放、侧边 PPT、笔记页。

## 目录

- `web/src/views/` — 页面
- `web/src/api/client.ts` — API 客户端

## 接口依赖

- `GET /api/v1/health`
- `GET/POST /api/v1/courses`
- （负责人提供）`GET /api/v1/courses/{id}/timeline`

## 验收

- `npm run build` 通过
- 能播放 mock 时间轴数据
